@model ProjectViewModel

<form asp-action="LinkProject" role="form">
    @await Html.PartialAsync("_ModalHeader", "Link Project")
    <div class="modal-body form-horizontal">
        <div class="row">
            <div class="col-lg-6">
                <div class="form-group">
                    <label asp-for="Id" class="col-lg-3 col-sm-3 control-label"></label>
                    @*<div class="col-lg-6">
                        <select asp-for="ProjectId" asp-items="@Model.Assignments" class="form-control">
                            <option>Please select</option>
                        </select>
                    </div>*@
                </div>
            </div>
           
        </div>
    </div>
    @await Html.PartialAsync("_ModalFooter")
</form>
